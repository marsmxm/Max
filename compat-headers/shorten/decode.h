/* Stub: shorten library not available for ARM64.
   ShortenDecoder is disabled at runtime. */
#ifndef SHORTEN_DECODE_H_STUB
#define SHORTEN_DECODE_H_STUB

typedef struct { int dummy; } shn_file;

#endif
